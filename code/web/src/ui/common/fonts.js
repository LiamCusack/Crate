// Fonts
// used to determine fonts, props passed to text styling
// such as H4 and p
// does p have a styling component?
// I don't think so

export const primary = `'Roboto', sans-serif`

export const secondary = `'Lobster', cursive`
